<p style="color: green"><%= notice %></p>

<%= render @patient_card %>

<div>
  <%= link_to "Edit", edit_patient_card_path(@patient_card) %> |
  <%= link_to "Back", patient_cards_path %>

  <%= button_to "Delete", @patient_card, method: :delete %>
</div>
